import { HighligightQuoteDirective } from './highligight-quote.directive';

describe('HighligightQuoteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligightQuoteDirective();
    expect(directive).toBeTruthy();
  });
});
